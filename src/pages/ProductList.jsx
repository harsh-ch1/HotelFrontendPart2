import React from 'react';
import Header from './Header';

const ProductList = () => {
    return (
        <div>
            < Header />
            <h1>product list page</h1>
        </div>
    );
};

export default ProductList;